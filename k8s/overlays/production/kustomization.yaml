apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# No Secret manifest here: both secrets (mcp-aiseg2-credential, mcp-github-oauth-aiseg2-eso) are
# delivered by External Secrets Operator from 1Password, declared on the fumo-k8s-platform side.
# The image tag is managed by ArgoCD Image Updater (git write-back to .argocd-source-aiseg2-mcp.yaml).
